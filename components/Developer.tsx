export default function Developer({ className }: any) {
  return (
    <a target="_blank" href="https://vk.com/rpoopr2005">
      <span className={`font-[500] hover:underline cursor-pointer text-amber-700 ${className}`}>
        {`<`}Разработчик{` />`}
      </span>
    </a>
  );
}
